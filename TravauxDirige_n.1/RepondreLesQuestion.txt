// Explication
// 

myvar = 0;
for ( i = 1; i < numtasks , ++i ) {
task = i;
MPI_Request req;
MPI_Status stat;
MPI_ISend(&myvar ,...,...,task ,&req);
myvar = myvar + 2;
// Do some works
...
MPI_Wait (req, stat);
}